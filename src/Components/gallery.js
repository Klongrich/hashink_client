import React from "react";

export default function Gallery() {

    return(
        <>
            <h2>This is where users can see there hashInk NFTs</h2>

            <h2>Might just make is so they can see all thier nft's</h2>

            <p>Can chose between etherscan API or instead opean-sea API
                I think either one is going to do the job
            </p>
        </>
    )
}